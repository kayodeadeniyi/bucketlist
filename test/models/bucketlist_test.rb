require 'test_helper'
require 'pry'
class BucketlistTest < ActiveSupport::TestCase
  test "A bucket is valid with a name and a user" do
    # user = User.new(email: "stephen@yahoo.com", password: "password", name: "stephen")
    # Binding.pry
    # user.save
    #
    # bucket = Bucketlist.new(name: "bucket", user_id: user.id)
    # bucket.save
    #
    # assert bucket.valid?
    # refute bucket.invalid?
  end
end
